package main

/*
func encode(n Neuron) int {

	w := int(((n.Weight + 4) / 8.0) * 65535)

	s := fmt.Sprintf("%02X%02X%02X%02X%03X", n.SourceLayer, n.SourceID, n.OutLayer, n.OutID, w)

	fmt.Printf("%+v encoded: %s\n", n, s)

	return 0

}
*/
